# config.env.example
# Example environment configuration for auto_dedupe
# Copy this to config.env and update paths as needed.

# Base directory for the project
BASE_DIR="/opt/auto_dedupe"

# Path to Python interpreter inside your venv
VENV_PY="$BASE_DIR/.venv/bin/python"

# Path to the main deduplication script
SCRIPT="$BASE_DIR/auto_dedupe.py"

# Input directory (where new photos/videos arrive)
INPUT="/mnt/storage/incoming"

# Output directory (where organized media is stored)
OUTPUT="/mnt/storage/media"

# Logging
LOG_DIR="$BASE_DIR/log"
LOG_FILE="$LOG_DIR/ingest_$(date +%Y%m%d_%H%M).log"

# Lock file (prevents overlapping runs)
LOCKFILE="$BASE_DIR/.auto_dedupe.lock"
